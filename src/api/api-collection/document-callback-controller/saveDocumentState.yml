info:
  name: saveDocumentState
  type: http
  seq: 1
  tags:
    - document-callback-controller

http:
  method: POST
  url: "{{baseUrl}}/api/document/callback"
  body:
    type: json
    data: |-
      {
        "room": "6db0aa7a-fc20-48e0-8484-7773b5b59e57",
        "data": "this is your content"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/document/callback/:docId"
      method: POST
      params:
        - name: docId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "unknown": ""
          }
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
