info:
  name: saveTemplate
  type: http
  seq: 2
  tags:
    - template-controller

http:
  method: POST
  url: "{{baseUrl}}/api/template"
  body:
    type: json
    data: |-
      {
        "name": "Test",
        "description": "Test",
        "content": "This is a tempalte"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/template"
      method: POST
      body:
        type: json
        data: |-
          {
            "id": "",
            "name": "",
            "description": "",
            "content": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "name": "",
            "description": ""
          }
