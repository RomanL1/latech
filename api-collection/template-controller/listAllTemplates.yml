info:
  name: listAllTemplates
  type: http
  seq: 1
  tags:
    - template-controller

http:
  method: GET
  url: "{{baseUrl}}/api/template"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/template"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          [
            {
              "id": "",
              "name": "",
              "description": ""
            }
          ]
