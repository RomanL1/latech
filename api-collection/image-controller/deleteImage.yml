info:
  name: deleteImage
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/api/document/:docId/images/:imageId"
  params:
    - name: docId
      value: "{{docId}}"
      type: path
    - name: imageId
      value: "{{imageId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
