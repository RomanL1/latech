info:
  name: getDocumentHistory
  type: http
  seq: 6
  tags:
    - document-controller

http:
  method: GET
  url: "{{baseUrl}}/api/document/:docId/history"
  params:
    - name: docId
      value: "{{docId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
