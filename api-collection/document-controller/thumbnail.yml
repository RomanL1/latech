info:
  name: thumbnail
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/document/:docId/thumbnail"
  params:
    - name: docId
      value: "{{docId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
