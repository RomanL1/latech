info:
  name: initiateDocumentRender
  type: http
  seq: 2
  tags:
    - document-controller

http:
  method: POST
  url: "{{baseUrl}}/api/document/:docId/render"
  params:
    - name: docId
      value: "{{docId}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/document/:docId/render"
      method: POST
      params:
        - name: docId
          value: ""
          type: path
    response:
      status: 200
      statusText: OK
      body:
        type: text
        data: ""
