info:
  name: getDocumentSecuredContent
  type: http
  seq: 3
  tags:
    - document-controller

http:
  method: POST
  url: "{{baseUrl}}/api/document/secured/:docId"
  params:
    - name: docId
      value: "{{docId}}"
      type: path
  body:
    type: json
    data: |-
      {
        "password": "PWS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/document/secured/:docId"
      method: POST
      params:
        - name: docId
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "password": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "id": "",
            "name": "",
            "secured": false,
            "content": ""
          }
